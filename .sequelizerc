const path = require('path');

module.exports = {
  "config": path.resolve('./config', 'connection.json'),
  "models-path": path.resolve('./core/domain-model'),
  "migrations-path": path.resolve('./core/migrations')
};