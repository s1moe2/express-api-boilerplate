language: node_js
node_js:
  - "10"
services:
  - postgresql
before_script:
  - cp src/config/config.js.travis src/config/config.js
  - psql -c 'create database api_test;' -U postgres
script:
  - npm run coverage-rep
  - codecov
env:
  global:
    - JWT_SECRET=nasifah8o7ryq34dbyi6rewie6td8y34bodq384ufao
    - JWT_TTL=12
    - MAILER_ENABLED=0
    - SPARKPOST_API_KEY=notakey
#after_success: 'npm run coveralls'
